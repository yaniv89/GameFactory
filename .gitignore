# --- JS / TS / pnpm ---
node_modules/
dist/
dist-app/
.vite/
.turbo/
*.tsbuildinfo
pnpm-lock.yaml.bak
coverage/

# --- .NET ---
bin/
obj/
*.user
.vs/

# --- Forge project working files ---
.forge/cache/

# --- Env / secrets ---
.env
.env.*
!.env.example
*.pfx
*.pem

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# --- Storybook / test artifacts ---
storybook-static/
playwright-report/
test-results/

# --- api-extractor scratch output (etc/*.api.md is the tracked report) ---
packages/*/temp/

# --- forge export's own per-project bundle inputs (never a checked-in
# fixture — scripts/generate-dev-fixture.mjs regenerates a real, working
# placeholder here on demand; a committed version would get silently
# overwritten by every local `forge export`/test run, dirtying the tree) ---
packages/player/src/generated/

