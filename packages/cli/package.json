{
  "name": "forge",
  "version": "0.0.0",
  "private": true,
  "description": "The forge CLI: local dev loop, CI helpers, bulk operations.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "forge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "node scripts/build-smoke-test.mjs && node dist/smoke/smokeTest.bundle.mjs"
  },
  "dependencies": {
    "@forge/player": "workspace:*",
    "@forge/project-export": "workspace:*"
  },
  "devDependencies": {
    "@forge/dialogue": "workspace:*",
    "@types/node": "^26.1.2",
    "esbuild": "^0.25.12"
  }
}
