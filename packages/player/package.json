{
  "name": "@forge/player",
  "version": "0.0.0",
  "private": true,
  "description": "The standalone, exported-game runtime — real sandboxed module loading (never packages/editor's directModuleHost.ts shortcut), localStorage saves, no calls back to Forge. What forge export bundles.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "node scripts/build-smoke-test.mjs && node dist/smoke/smokeTest.bundle.mjs"
  },
  "dependencies": {
    "@forge/core": "workspace:*",
    "@forge/module-api": "workspace:*",
    "@forge/render-2d": "workspace:*",
    "@forge/runtime-host": "workspace:*",
    "pixi.js": "^8.19.0",
    "quickjs-emscripten": "^0.32.0"
  },
  "devDependencies": {
    "@forge/dialogue": "workspace:*",
    "@types/node": "^26.1.2",
    "esbuild": "^0.25.12"
  }
}
